export const slideDown = {
    in: { opacity: 1, transform: 'translateY(15px)' },
    out: { opacity: 0, transform: 'translateY(-15px)' },
    common: { transformOrigin: 'top' },
    transitionProperty: 'transform, opacity',
}
